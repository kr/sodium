inline C <<end
#include <string.h>
#include "st.h"
end

def symbol ():
  inline C str <<end
    const char *cont;
    size_t len;

    cont = symbol2charstar(rcv);
    len = strlen(cont);
    return make_str_init(len, len, cont);
  end

def install-symbol ():
  inline C (run sur) <<end
    symbol_surrogate = n_sur;
    return ok_sym;
  end

install-symbol symbol

. '()
