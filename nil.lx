
def nil ():
  (str) "()"
  (filter f) '()
  (run f) 'ok
  (schedule f) 'ok
  (= x) (is? x nil)
  (assq name) '()

def install-nil ():
  inline C (run sur) <<end
    nil_surrogate = n_sur;
    return ok_sym;
  end

install-nil nil

. '()
